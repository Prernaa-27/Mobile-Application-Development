<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FAF7F8"
    android:padding="16dp"
    android:paddingTop="36dp">
    <!-- top bar with back, title and camera button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp">
        <!-- transparent so it looks like a plain icon -->
        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="#00000000"
            android:src="@android:drawable/ic_media_previous"
            android:contentDescription="Back button"/>
        <!-- weight=1 pushes the two buttons to either side -->
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Photo Gallery"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="#1F1F1F"
            android:gravity="center"/>
        <ImageButton
            android:id="@+id/btnCamera"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="#00000000"
            android:src="@android:drawable/ic_menu_camera"
            android:contentDescription="Camera button"/>
    </LinearLayout>
    <!-- only visible when the folder has no images -->
    <TextView
        android:id="@+id/tvEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No images found"
        android:textSize="16sp"
        android:textColor="#777777"
        android:gravity="center"
        android:visibility="gone"
        android:layout_marginTop="24dp"/>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewImages"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
</LinearLayout>